#include <stdio.h>
#include <stdlib.h>

#define MAX 100

typedef struct materie
{
    char nume[100];
    int ore_curs;
    char tip_examen;
};

void citire_MAT(materie *nume, materie *ore_curs, materie *tip_examen)
{
    scanf("%c", &nume);
    scanf("%d", &ore_curs);
    scanf("%c", &tip_examen);
}

void citire_programa(materie nume)
{
    int i;
    for (i = 0; i <= M)
}

int main()
{
    int programa[MAX];
}